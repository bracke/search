# Public API

Use `Search.Queries.New_Builder`, setter procedures, and `Build` to create an immutable query. Validate with `Search.Queries.Validate`. Execute with `Search.Engine.Execute`. Export with `Search.Exports.Render`.
