# Search Semantics

Each filesystem object appears at most once per result set. Content matches are attached to the object result. Result identity is stable within a session and revisions describe refreshed state.
