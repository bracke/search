# User Guide

Search modes are filename, path, content, and combined. Literal searches are case-insensitive by default. Regular-expression searches use the `regexp` crate. `.gitignore` filtering is enabled by default through `versionlib`.
