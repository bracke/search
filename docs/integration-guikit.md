# Integration With guikit

`Search_GUI.To_Grid_Items` maps result rows into `Guikit.Item_Grid.Layout_Item` values. Commands expose localization keys and stable identifiers suitable for guikit command surfaces.
