# Testing

All automated tests are AUnit suites in `search_tests`. Coverage includes query validation, traversal, content search, regexp integration, localization keys, settings, persistence models, GUI model adapters, execution, cancellation, exports, and release checks.
